apiVersion: v1
kind: Service
metadata:
  name: apache-service
  namespace: {{ .Values.namespace }}
  labels:
    app: apache-php
    tier: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: apache-php
    tier: frontend